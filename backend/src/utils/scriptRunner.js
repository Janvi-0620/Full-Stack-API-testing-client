/**
 * Phase 2: pre-request scripts and tests in isolated VM.
 * Stub for MVP.
 */
export async function runScript(_source, _context) {
  return { logs: [], error: 'Script runner not enabled in MVP' };
}
